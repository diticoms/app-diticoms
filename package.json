
{
  "name": "diticoms-service-manager",
  "version": "1.0.45",
  "description": "Hệ thống quản lý dịch vụ sửa chữa Diticoms - Web & PWA",
  "scripts": {
    "start": "echo 'Ứng dụng đang chạy trên môi trường Web...'",
    "build": "node build.js",
    "version-up": "node -e \"const fs = require('fs'); const v = JSON.parse(fs.readFileSync('version.json')); const parts = v.version.split('.').map(Number); parts[2]++; v.version = parts.join('.'); fs.writeFileSync('version.json', JSON.stringify(v, null, 2)); console.log('Version: ' + v.version);\"",
    "deploy": "npm run build && npm run version-up && git add . && git commit -m \"Web Deploy: Cập nhật hệ thống\" && git push origin main"
  },
  "keywords": [
    "diticoms",
    "service",
    "manager",
    "pwa"
  ],
  "author": "Diticoms",
  "license": "MIT",
  "dependencies": {
    "lucide-react": "^0.563.0",
    "html2canvas": "^1.4.1",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "esbuild": "^0.24.0",
    "xlsx": "^0.18.5"
  }
}
